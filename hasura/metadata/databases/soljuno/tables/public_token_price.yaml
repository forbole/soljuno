table:
  name: token_price
  schema: public
object_relationships:
- name: token_unit
  using:
    foreign_key_constraint_on: id
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - price
    - market_cap
    - symbol
    - timestamp
    filter: {}
  role: anonymous