table:
  name: token_supply
  schema: public
object_relationships:
- name: token
  using:
    manual_configuration:
      remote_table:
        schema: public
        name: token
      column_mapping:
        address: address
select_permissions:
- permission:
  allow_aggregations: true
  columns:
    - address
    - slot
    - supply
    filter: {}
  role: anonymous